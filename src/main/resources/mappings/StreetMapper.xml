<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tinkoff.tourism.dao.sights.StreetRepository">

    <insert id="addSight" parameterType="tinkoff.tourism.model.sights.Street">
        INSERT INTO streets (name, type, xCoordinate, yCoordinate, description, siteLink, openTime, closeTime, price, history)
        VALUES (#{name}, #{type}, #{xCoordinate}, #{yCoordinate}, #{description}, #{siteLink}, #{openTime}, #{closeTime}, #{price}, #{history})
    </insert>

    <select id="findAll" resultMap="StreetMap">
        SELECT *
        FROM streets
    </select>

    <select id="findById" resultMap="StreetMap">
        SELECT *
        FROM streets
        WHERE streets.id = #{id}
    </select>

    <select id="findByName" resultMap="StreetMap">
        SELECT *
        FROM streets
        WHERE streets.name = #{name}
    </select>

    <update id="updateSight" parameterType="tinkoff.tourism.model.sights.Street">
        UPDATE streets
        SET NAME        = #{name},
            TYPE        = #{type},
            XCOORDINATE = #{xCoordinate},
            YCOORDINATE = #{yCoordinate},
            DESCRIPTION = #{description},
            SITELINK = #{siteLink},
            OPENTIME = #{openTime},
            CLOSETIME = #{closeTime},
            PRICE = #{price},
            HISTORY = #{history}
        WHERE ID = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE
        FROM streets
        WHERE ID = #{id}
    </delete>

    <delete id="deleteAll">
        DELETE
        FROM streets
    </delete>

    <resultMap id="StreetMap" type="tinkoff.tourism.model.sights.Street">
        <result property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="type" column="type"/>
        <result property="xCoordinate" column="xCoordinate"/>
        <result property="yCoordinate" column="yCoordinate"/>
        <result property="description" column="description"/>
        <result property="siteLink" column="siteLink"/>
        <result property="openTime" column="openTime"/>
        <result property="closeTime" column="closeTime"/>
        <result property="price" column="price"/>
        <result property="history" column="history"/>
    </resultMap>

</mapper>